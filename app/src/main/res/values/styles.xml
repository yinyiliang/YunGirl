<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="FirstTheme" parent="Theme.AppCompat">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@mipmap/org_bg5_242</item>
    </style>

    <!--Toolbar主题-->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="ViaTextAppearance">
        <item name="android:textColor">@color/tingeColor</item>
        <item name="android:textSize">@dimen/apace_12</item>
    </style>

    <!--PopupWindow出现和消失动画效果-->
    <style name="PopupWindow_anim_style">
        <item name="android:windowEnterAnimation">@anim/popupshow_anim</item>
        <item name="android:windowExitAnimation">@anim/popuphide_anim</item>
    </style>

</resources>
